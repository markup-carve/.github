name: Discord good first issues

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: discord-good-first-issues
  cancel-in-progress: false

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Restore notification state
        uses: actions/cache/restore@v4
        with:
          path: .bridge-state/good-first-issues.json
          key: discord-good-first-${{ github.run_id }}
          restore-keys: discord-good-first-
      - name: Post new labeled issues
        env:
          GH_TOKEN: ${{ github.token }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_GOOD_FIRST_ISSUES_WEBHOOK }}
        run: python3 .github/scripts/discord_good_first_issues.py
      - name: Save notification state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .bridge-state/good-first-issues.json
          key: discord-good-first-${{ github.run_id }}
